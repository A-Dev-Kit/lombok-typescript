# Use pnpm for development
# Consumers can use any package manager (npm, yarn, pnpm)

# Hoist all dependencies for compatibility
shamefully-hoist=true

# Strict peer dependencies
strict-peer-dependencies=false

# Auto-install peers
auto-install-peers=true

# Use lockfile version 3 for better compatibility
use-lockfile-v6=true

